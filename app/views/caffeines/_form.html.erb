<%= form_for(@caffeine) do |f| %>
  <% if @caffeine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@caffeine.errors.count, "error") %> prohibited this caffeine from being saved:</h2>

      <ul>
      <% @caffeine.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
